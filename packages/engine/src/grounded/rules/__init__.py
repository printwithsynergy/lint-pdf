"""Rule engine for conditional check evaluation.

The rule engine supports per-check conditions that control when
a check applies and allows parameter/severity overrides based on
document context (page number, object type, layer name, etc.).
"""
