[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/engine/Dockerfile"

[deploy]
numReplicas = 1
startCommand = "sh -c 'celery -A grounded.queue.worker worker --loglevel=info --queues=default,priority --concurrency=${CELERY_DEFAULT_CONCURRENCY:-2}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
