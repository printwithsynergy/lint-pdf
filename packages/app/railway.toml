[build]
builder = "RAILPACK"

[deploy]
startCommand = "cd packages/database && npx prisma db push --accept-data-loss && cd ../app && next start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
