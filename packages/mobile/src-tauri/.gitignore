# Tauri build artifacts
/target/

# Mobile platform scaffolding generated by `tauri ios init` and
# `tauri android init`. Each developer regenerates locally — checking
# in machine-specific paths and Xcode project metadata creates noisy
# diffs and cross-platform breakage.
/gen/
