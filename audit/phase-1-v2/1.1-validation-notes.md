# Phase 1.1 — Validation Notes (v0 output limitations)

**Date:** 2026-04-25
**Source:** spot checks against v1 closed gaps and analyzer code
**Status:** v0 baseline — needs verification sweep before Phase 1.2 locks

## What the agent produced

`audit/phase-1-v2/v2-id-coverage.json`:
- 364 user-facing rows (universe says 328 → +36 over-generation; or, by per-prefix sum of universe §10.0, 446 → −82 under-generation; the universe itself is internally inconsistent on totals — see below)
- 82 Tier-0 primitive rows (universe says 84 → −2)
- Status counts: 54 present / 0 partial / 310 absent (user-facing); 41 / 0 / 41 (Tier-0)

## Internal universe inconsistency

The v2 universe enumeration §1 (tier breakdown summary) and §2 (per-prefix
counts) **don't sum to the same total**:
- §1: T0(84) + T1(62) + T2(101) + T3(48) + T4(74) + T5(43) = **412**
- §2 sum: 41+67+38+22+36+19+34+21+23+16+9+28+12+18+7+11+10+12+22 = **446**

The §1 summary excludes some IDs (likely the T=0 primitive rows embedded
within prefix tables, e.g., F-03 "Font fully embedded vs subset" which is
tagged T=0). 446 − ~84 dual-counted Tier-0 in prefix tables = ~362 ≠ 328.

This is a playbook documentation inconsistency, not an agent error. Phase
1.2 will treat per-row data as authoritative.

## Concrete false negatives detected

Spot checks against v1's closed gap-mapping + analyzer source:

| v2 ID | Agent verdict | True state | Evidence |
|-------|---------------|------------|----------|
| F-22 | absent | **PRESENT** | v1 font.py has min-size emit; agent missed `LPDF_TEXT_002` or similar |
| F-26 | absent | **PRESENT** | v1 closed via white text overprint check; agent missed |
| D-01 | absent | **PRESENT** | dieline.py 894 LOC detects spot-name dielines; v1 closed gap T3-D02 |
| LA-01 | absent | **likely PRESENT** | v1 hairline.py emits LPDF_STROKE_*; agent missed |

The pattern: agent did NOT fully traverse v1's gap-mapping.json (which has
match_details for each closed gap → engine inspection_id). v1's 93 closed
gaps map to ~62 distinct engine inspection_ids (per v1 phase-1-summary.md),
so at minimum 62 v2 IDs should be flagged present. Agent reports 54
present → close in count but distribution differs.

## What's still useful from v0 output

1. **Tier-0 primitives breakdown (41/82 present)** — confirms Phase 0.2's
   finding that "no central registry exists; predicates are scattered."
   Direction is right; specific 41 vs 41 split is plausible per analyzer
   code spread.
2. **Wave assignments per row** — internally consistent with the universe
   table's W column.
3. **Prefix-level coverage rankings** — directionally correct (B/I strong,
   D/W/BR/L/EPM weak).
4. **EPM = 0/22 present** — accurate; EPM module is greenfield.
5. **Regulatory R-* = 0/10 present** — accurate at v2 R-* level even though
   v1 has AI regulatory analyzers (those are unmapped-existing per Phase
   0.2 taxonomy collapse).

## What's NOT trustworthy

1. Per-row absent classifications for v1-covered checks (false negatives)
2. Aggregate "present" count of 54 — likely under-count by 5-15
3. By-tier counts (T1=3 looks wrong; many Tier-1 IDs from F-/C-/P- are
   actually marked T=1 in the universe and ARE covered by v1)

## Recommended remediation

**Targeted verification sweep** before Phase 1.2 backlog locks.

Approach:
1. Read v1 gap-mapping.json — extract all 93 gaps + their matched
   inspection_ids (~62 unique)
2. For each engine inspection_id covered by v1, find the v2 ID(s) that
   semantically map to it
3. Update v2-id-coverage.json: any v2 ID matching a v1-covered
   inspection_id and currently flagged absent → upgrade to present (or
   partial if the v1 implementation is acknowledged-incomplete)
4. Re-run aggregate counts

This is a ~2–3 hour exercise vs re-running the full agent (which would burn
similar time + tokens for unclear improvement). Recommended: do this as
Phase 1.1b verification pass, then proceed to 1.2 backlog with the
corrected JSON.

Alternative: accept v0 as-is and let Phase 1.2 / Phase 2 design notes flag
the false negatives during per-check design ("oh this is actually
implemented as `LPDF_X` — re-classify in the design note"). Riskier;
backlog priority scoring may misallocate effort to "absent" items that
are actually done.

## Decision needed from operator

- **(a)** Run the verification sweep before Phase 1.2 (recommended)
- **(b)** Accept v0; flag during Phase 2 design notes
- **(c)** Re-run the agent with stricter prompt (slowest)

Surfaced in End-of-Phase-1 Q&A gate.
